# Point this at a real burger model to replace the procedural one.
# Put the file in public/models/ and reference it by public path.
#
#   NEXT_PUBLIC_BURGER_GLB=/models/burger.glb
#
# Leave unset to use the built-in procedural burger.
NEXT_PUBLIC_BURGER_GLB=
